package com.example.to_dolistapp.presentation.screens

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.navigation.NavController

@Composable
fun CompletedScreen(navController: NavController) {
    Text(text = "Completed Tasks Screen (Placeholder)")
    // TODO: Show tasks that have isCompleted = true, or separate object
}
